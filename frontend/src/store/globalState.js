import state from './global-state/state'
import getters from './global-state/getters'
import mutations from './global-state/mutations'
import actions from './global-state/actions'

export default {
  state: state,
  getters: getters,
  mutations: mutations,
  actions: actions
}
